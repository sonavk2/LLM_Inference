# Instruction-tuned LLM slot for Phase 1.
# Llama-3.1 (not 3.0) because 3.0 is only 8k native and would silently produce
# garbage past position 8192 at the long-context cells.
#
# NOTE: gated on Hugging Face. Run `huggingface-cli login` and accept the
# license at https://huggingface.co/meta-llama/Meta-Llama-3.1-8B-Instruct
# before the first run.

model_id: meta-llama/Meta-Llama-3.1-8B-Instruct
dtype: bfloat16
trust_remote_code: false
native_context: 131072
